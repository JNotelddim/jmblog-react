import { styled } from '@material-ui/core';

export const Base = styled('div')({
  maxWidth: '800px',
  margin: '0px auto',
  height: 'calc(100% - 60px)', // 60px here is the height of the header bar
});
